Informe

En la estructura que maneja procesos y prioridades en proc.h 
agregamos “int prioridad;” y “int boost;” luego vamos a proc.c 
que es donde los procesos se inicializan, dejamos establecido
los valores de prioridad y boost, para despues ir a la función.

Ahora cambiamos el scheduler para que tenga una modalidad 
que considere tanto prioridad como boost por lo que cambiamos 
en proc.c la parte de shceduler(void) , agregando un loop
que recorre todos los procesos del sistema, luego verifica si
el proceso esta en estado runnable, evitando a los que no estan 
en este estado, si lo esta se actualiza su prioridad y ajusta 
el valor del boost

Luego creamos el archivo dentro del directorio user, 
que haga la prueba de prioridades con 20 procesos el archivo 
crea un bucle con 20 procesos hijos con fork los cuales en 
la interfaz se ve como se van ejecutando cada uno

y por ultimo agregamos la funcion para ejecutar el test de 
prioridad en el makefile